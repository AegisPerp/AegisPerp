# AlphaPerp — free HTTPS via Caddy + Let's Encrypt (auto certificate).
# 1) Ganti "alphaperp.example" dengan domain kamu (A record → 194.233.84.10).
# 2) Install Caddy (sekali):
#      sudo apt install -y debian-keyring debian-archive-keyring apt-transport-https curl
#      curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/gpg.key' | sudo gpg --dearmor -o /usr/share/keyrings/caddy-stable-archive-keyring.gpg
#      curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/debian.deb.txt' | sudo tee /etc/apt/sources.list.d/caddy-stable.list
#      sudo apt update && sudo apt install -y caddy
# 3) Pakai file ini:
#      sudo cp deploy/Caddyfile /etc/caddy/Caddyfile && sudo systemctl reload caddy
# 4) Buka firewall 80 + 443 (ufw + panel VPS). HTTPS otomatis aktif.

alphaperp.example {
	encode zstd gzip
	reverse_proxy localhost:3456
}

# (opsional) redirect www → root
www.alphaperp.example {
	redir https://alphaperp.example{uri} permanent
}
